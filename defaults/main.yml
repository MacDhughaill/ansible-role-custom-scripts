---
custom_scripts:
  - src: "{{ custom_scripts_default_src }}"
    dest: "{{ ansible_user_dir }}/bin"
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_id }}"
    mode: u+x
#  backup:
# custom_scripts_default_src:
